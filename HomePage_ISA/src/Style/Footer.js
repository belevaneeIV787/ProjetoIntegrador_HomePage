import styled from "styled-components"

export const BoxFooter = styled.section`
    height: 10%;
    background-color: #130404;
    display: flex;
    justify-content: center;
    align-items: center;
    bottom: 0;
    position: fixed;
    width: 100%;
    text-align: center;
`

export const TextosFooter = styled.h3`
    color: #D4C9BE;
    padding-left: 50px;
  
    font-weight: 500;
    font-size: clamp(12px, 1em + 4vw, 8px);
`